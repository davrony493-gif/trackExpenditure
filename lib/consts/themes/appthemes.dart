import 'package:flutter/material.dart';
import 'package:track_expenses/consts/colors/appcolors.dart';

class Appthemes {
  static final ThemeData light = ThemeData(colorScheme: ColorScheme.light(primary: Appcolors.black));
   static final ThemeData dark = ThemeData(colorScheme: ColorScheme.dark(primary: Appcolors.white));
}
